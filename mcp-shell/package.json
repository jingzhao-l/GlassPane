{
  "name": "glasspane-mcp",
  "version": "1.1.1",
  "description": "GlassPane MCP stdio shell: JSON-RPC 2.0 over stdio bridging an AI agent to the glasspane engine daemon",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "glasspane-mcp": "dist/index.js"
  },
  "files": [
    "dist",
    "schemas",
    "LICENSE",
    "README.md"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "bundle": "esbuild src/index.ts --bundle --platform=node --format=esm --target=node18 --external:zod --outfile=dist/index.js && rm -f dist/*.js.map dist/*.d.ts.map && find dist -maxdepth 1 -name '*.js' ! -name 'index.js' -delete && rm -rf schemas && mkdir -p schemas && cp -R ../kernel/schemas/. schemas/",
    "test": "npm run build && node --test test/*.test.mjs",
    "prepublishOnly": "npm run build && npm run bundle"
  },
  "license": "MIT",
  "author": "jingzhao-l",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jingzhao-l/GlassPane.git",
    "directory": "mcp-shell"
  },
  "bugs": { "url": "https://github.com/jingzhao-l/GlassPane/issues" },
  "homepage": "https://github.com/jingzhao-l/GlassPane/tree/main/mcp-shell#readme",
  "keywords": ["mcp", "model-context-protocol", "macos", "accessibility", "verification", "ai-agents"],
  "dependencies": {
    "zod": "3.25.76"
  },
  "devDependencies": {
    "@iterate/kernel": "file:../kernel",
    "@types/node": "26.5.1",
    "esbuild": "^0.28.2",
    "typescript": "5.9.3"
  }
}
