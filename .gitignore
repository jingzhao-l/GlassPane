.DS_Store
node_modules/
dist/
*.tsbuildinfo
.build/
__pycache__/
.pytest_cache/

# spike/冒烟在仓库根构建出来的 app 产物（如 FineTuneRetest.app）不入库
/*.app/

# 打包时从 kernel 复制的 schema 真源（bundle 步骤产物）
/mcp-shell/schemas/

# npm pack 产物（发布用，不入库；历史上误提过一枚 glasspane-mcp-shell-0.1.0.tgz）
*.tgz

# 上游参照克隆（223 MB，含 .git）；fork 的 diff 尺子读它，绝不进版本控制
.external/
