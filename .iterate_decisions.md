# Iterate Decision Log

Goal: 全面审查并迭代本项目目前所有代码实现（已提交状态）
Max rounds: 3
Started: 2026-09-22T21:15+08:00
Branch: `iterate/full-review-20260922` @ worktree `/Volumes/Eng-Dev/.worktrees/gp-iterate`
Baseline commit: `6f5c0c7`（= origin/main，本轮开始时用户本地 main 停在 0340e6a，落后 8 笔）
Enabled dimensions: correctness · security · architecture · spec-compliance · frontend-backend · tech-debt
Gate set: 镜像 `.github/workflows/ci.yml`（engine build+test / probe test / signal smoke / bridge pytest / kernel / mcp-shell / installer / root workspace）

---

## Setup 决策

| # | 决策 | 理由 |
|---|---|---|
| S-1 | 基线用 origin/main（6f5c0c7）而不是用户工作副本 HEAD（0340e6a） | 提问期间另一会话连推 8 笔（v1.1.0 统一版本线、发布形状、CI/release）；审 0340e6a 等于审过期代码，且会与那 8 笔在 `installer/cli.js`、`install.sh` 上必然冲突 |
| S-2 | 全程在独立 worktree 工作，`/Volumes/Eng-Dev/GlassPane` 一个字节不动 | 用户选择「只审已提交 HEAD」；工作副本内有另一会话 27 项未提交改动（面板控制台批次），staging 它就是「混合暂存」反例（见项目记忆 parallel-worktree-isolation） |
| S-3 | gate 一律用隔离 `HOME=/var/tmp/gp-iterate-home` 跑 | HEAD 上 A-1（`swift test` 污染真实 `~/.glasspane/projects.json`）尚未修，直接跑会往用户活注册表写假项目；基线跑完 `shasum -c` 已验证真实文件未变 |
| S-4 | 已知清单不进 findings 池：`specs/GlassPane_Audit_2026-09-22` §4（A-13…A-22、B-4…B-24、C-3…C-5、D、B1/B2）作为**修复队列**，reviewer 只补新发现 | 避免把同一批缺陷报两遍占掉 findings 名额；reviewer 仍被要求回报「HEAD 实况与清单不符」的差异 |
| S-5 | A-1…A-12 / B-1…B-3 / C-1…C-2 标 `covered-by-wip` 跳过 | 这些修复只存在于另一会话未提交的工作副本；在 HEAD 分支上重做 = 必然冲突且重复劳动 |
| S-6 | 风险区（`installer/`、`install.sh`、`.github/`、`README*`）默认只报不改 | 另一会话 21:00–21:16 刚改过这些文件，仍在活跃期；改这里制造合并冲突而非价值 |
| S-7 | 授权口径 | 沿用 2026-09-20 的全量授权：架构性问题当轮直接修，不再逐批问；merge / push / npm publish / 真实删除仍需口令 |

## Round 1 — 审查完成（21:15–21:55），修复进行中

Phase 1：6 维 × 7 个只读 reviewer（分区不相交），覆盖 27,259 行 / 137 源文件 + 12 个规格小节。
产出 **111 条新 finding**（critical 5 / high 32 / medium 43 / low 31），已连同 §4 已知队列合并进
`/var/tmp/gp-iterate-gates/round1-queue.md`（171 行工作索引）。

**证据门禁（机械化）**：`verify-anchors.py` 逐条解析 110 个 `file:line` 锚点 → 文件存在 + 行号在范围内 + 打印实际源码行供人核。结果 `CHECKED 110 anchors, EVIDENCE_VIOLATION 0`；抽查确认语义正确（R1-02→`public let bounds: Bounds?`、R3-01→writeFrame 的 `write`、R6-09→`_ = chmod`）。覆盖率自报：R1 12/12、R2 26/26、R3 16/16、R4 19/19、R5 27/27、R6 24 节/78 要求、R7 29/29 —— 无 COVERAGE_GAP。

### 交叉印证出的三条最重要的（多个独立 reviewer 各自命中）
1. **A-13 + R1-02 是同一条因果链的两半**：kernel 读侧只认冻结 const（拒绝 `0.1-draft`）**且** Swift 合成编码器在 `pixelDiff.bounds==nil` 时省略该键（schema 要求存在）→ 磁盘 561 条档案里 draft 的读不回、零像素变化的读不回，而 shell 把两种情况都说成 `engine and kernel schema drifted`。R4 还实测 draft 档案结构完整 → 读侧兼容即可，无需放宽别的。
2. **`attach.pid` 无范围校验**（R2-01 与 R4-01 各自独立发现，两侧同缺）：`pid_t($0)` 是会 trap 的 Int32 转换，一个 advertise 过的调用形状即打死 daemon。
3. **R7-00 元问题**：审计在档 §3「本轮已修」的 A-1…A-12/B-1/C-1/C-2 与 §2 整个四页控制台、§3.2 daemon 写入面**在 HEAD 全部不存在**（R1/R2/R5/R7 四个独立 reviewer 分别撞到）。按 S-5 我不重做，但它意味着**在档验证记录描述的是另一棵未提交的树**——这必须是收尾报告第一条。

### Phase 2/3 修复波次（12 个并行 fixer，文件集互不相交；中央一次 gate）
wave 1：A 探针 SDK · B EngineCore 诚实面 · C 契约读侧+编码器 · E 探针服务面 · F 参数与出站帧 · G LLDB 桥 · B3 假绿冒烟闸门
wave 2：P2 面板 · P5b socket/daemon/CLI · P7a stdio 运行时 · P7b 路径与档案删除半径 · P7c 报告双渲染器与 golden
待用户裁决（不动）：R7-00 口径 · R6-07 审批链尾切 · R6-12 第 14 个 MCP 工具合法化 · R4-02/R6-01 recipe 契约统一到哪侧 · 风险区（installer/.github/README/install.sh）全部条目
延后到 round 2/3：R7-02+B2 测试隔离闸（需先看 W1-B 的注入点需求） · R7-04 死项目 API 三件套 · R7-09/10/11/12 · R6-10 单客户端与面板并存 · R2-05/06/08/10/11/12 与全部 medium/low 余量

## ⚠ 事故记录 — 我毁掉了开发者的真实注册表（2026-09-23 07:11 / 07:13）

跑 `swift test` 时，真实 `~/.glasspane/projects.json` 被覆写：**71 条 → 2 条合成条目**
（`Proj` / `com.example.app`）。原因：我用 `HOME=/var/tmp/gp-iterate-home` 当作沙箱，但
Foundation 的 `NSHomeDirectory()` 仍解析到 `/Users/ethanlin`；根因是 HEAD 上未修的测试隔离缺陷
（在档 A-1 + 本轮 R7-02：`EngineP1Batch6Tests:170` 用默认路径构造 `ProjectRegistry()`、
`makeCore(directory: nil)` 流向 `EvidenceStore.defaultDirectory`）。
我的守卫是**事后** `shasum -c`：07:11 报了 CHANGED，但写入已发生 → 事后检测不构成防护。
不可恢复：无 APFS 本地快照、`tmutil destinationinfo` 无目标、`~/.glasspane` 无 `.orig/.tmp` 残留。
未受影响：`approvals.json`（9-21 时间戳）、证据档案（仅多出 2 枚测试写入的 `op_*.json`）。
处置：① 隔离改为结构性（W5-A，见下）；② 快照现存状态作 canary
（`/var/tmp/gp-iterate-gates/state-backup/projects.json.0713`，sha256 `5691fffa…`；evidence 582 枚）；
③ 每次 gate 后复核 canary，且在隔离被证明前不再跑套件。
教训已写进长期记忆：`feedback-prove-isolation-before-suite.md`。

## W5-A — 测试隔离结构性化（R7-02 + B2）

`TestSandbox`（TestSupport.swift，注入路径强制非可选）+ `EngineP1Batch6Tests` 全量改指 temp +
新 `TestIsolationGateTests.swift`（11 条文本禁形 + 运行时"路径必须都在 `NSTemporaryDirectory()` 内"
双轨；needle 用运行时拼接避免自匹配；不跳过自身文件；源码缺失 fail-closed；
`testScannerRejectsTheForbiddenShapes` 把 11 条规则各自喂回合成行证明闸**能红**）。
`testCoreWithoutArchiveStillWorksInMemoryOnly` 从名不副实变成真断言（`archive:false` 不交 store +
注入目录 `contentsOfDirectory == []`）。

### 已知残留（本轮未修，须记录）
- `EngineCore.swift:117` `projectRegistry ?? ProjectRegistry()`：任何省略该参数的构造仍会**打开**真实
  projects.json（只读；写需 `projectSet`，现无此类调用者）。文本闸不能一律禁（会红掉几十个测试），
  需要把默认改成注入必填或显式 nil —— 归 round 2 的 EngineCore 批。
- `PermissionSubjectTests.swift:636` 在真实家目录根下建 scratch 目录（R5-04 需要"在 $HOME 内"这一支
  才能测；有 defer 清理，不碰 `.glasspane`）。可改用 `isHomeScopedPath(resolved:home:)` 的注入 home。
- `ApprovalGate()` 经核对不是污染源（path 默认 nil = 纯内存），闸刻意不禁它。

## Round 0 — 基线与 onboarding（21:15–）

新增文件：`ITERATE.md`（项目知识库）、`iterate.config.yaml`（6 维 + 镜像 CI 的 validation.commands + 个性化约束）。

### 基线 gate（HEAD 6f5c0c7，全部实测）

| 模块 | 命令 | 结果 |
|---|---|---|
| engine build | `swift build`（冷跑，隔离 HOME） | Build complete! (309.13s)，exit 0 |
| engine test | `swift test` | 见下表（跑动中） |
| bridge | `python3 -m pytest -q` | 17 passed，exit 0 |
| kernel | `npm test` | exit 0（明细见日志） |
| mcp-shell / installer / probe | 各自 gate | 跑动中 |
| 真实注册表未被污染 | `shasum -a 256 -c projects.json.baseline.sha` | OK（哈希 = 3c45a518…cc55） |

## Round 1 / Round 2 收口（提交 90f3255 → dfaca4d → 3174416）

- **Round 1**：16 批修复提交 `dfaca4d`（71 文件 / +13,975−1,312）。gate 全绿：`swift test` **509 通过 0 失败 1 跳过**（基线 374）、probe 18、bridge 46、kernel/mcp-shell/installer/root/signal 全绿。
- **Round 2**：对 `dfaca4d` 的 diff 做全新视角复审（3 只读 reviewer）→ **33 条新发现**，其中 2 条是我在 round 1 把事改坏（`.p6` 标签全等比较致唯一端到端闸永久红；A-17 修法把单连接卡升级为守护级卡）。修复后提交 `3174416`，工作树净。
- **最终 gate（09:14 那次，CI 等价 9 模块）**：engine build ✅ / `swift test` **530 通过 0 失败 1 跳过** ✅ / probe 24 ✅ / signal ✅ / bridge 50 ✅ / kernel ✅ / mcp-shell ✅ / installer ✅ / root workspace ✅；`GUARD real_projects_json=unchanged`（隔离成立，非假设）。
- 完整发现账本与理由：`/var/tmp/gp-iterate-gates/round1-queue.md`、`round2-queue.md`、批次归属 `batch-files.txt`、gate 日志 `/var/tmp/gp-iterate-gates/runs/`。
- 未收口/需裁决清单见 `round2-queue.md` 末尾（X-22 死闸、rejectedKeys 无读者、两条合流冲突、Swift/TS 路径校验不一致、规格字面量漂移等）。**分支未 merge、未 push。**


## Round 3 — 收敛复审与收口（2026-09-23 09:30–10:10）

Round 3 全新视角复审 verdict = **needs-round-3**，4 条发现。逐条实况（不是"计划修"）：

1. **`--no-c33` ⇒ `.strong` 不可达**：round 2 我把"未装 tap"与"tap 中途死"统一成 `.weak/contaminated:true` 之后，`.p6_smoke.py` 要求的 `.strong` 档在 `--no-c33` 启动的守护进程上**永远测不出来** —— 这是本轮第二个"永久红"闸（第一个是 round 1 我引入、round 2 已修的标签全等比较）。它不是代码 bug，是 **P6 §2.3 与 P2 v2.0 §17.2 的降级口径冲突**（两者给同一退化同一判决），改哪一侧都是产品/规格决定 → **交回用户，本轮不动**。
2. **HIGH（已修）**：`EngineCore.pruneEvidence(projectId: nil)` 在**没有注入 store** 的实例上回落到 `EvidenceStore.defaultDirectory` 并删除过期条目 —— 即 07:13 事故路径的最后一半（它靠"少写一个参数"抵达，任何路径字面量闸都看不见）。现在：无 store ＝ 无磁盘副作用（`EngineCore.noStoreArchiveRefusal`，`GP_E_BAD_PARAMS`），共享档案的修剪明确归 `glasspaned --prune-evidence`（该进程才拥有 `~/.glasspane`，其构造点改为显式 `EvidenceStore.atProductionDefault()`）；`TestIsolationGateTests` 加 **pair 规则**（同文件同时出现 `evidenceStore: nil` 与 `pruneEvidence(projectId: nil` 即报），并用一行式 + 跨行式两个合成样本自证能红 + 归因断言 + 两条合法形状必须干净；`testPruneEvidenceProjectIsolation` 从"不注入 store"改成注入**第三个**临时档案，同一句话变成更强的证明。
3. **MEDIUM（已修）**：两处 `rollback_full` remedy 教 agent 去读 `disconnections` 与"每行的 `connected` 标志"，而 A-10 之后前者是纯整数、后者全是 `true`（断开记录已移到 `recentDisconnections`）→ 照做会得出"探针活着"。文案改为指名 `recentDisconnections`（`disconnectReason`/`disconnectedAt`/`drops`）并说明 `probes` 只含活注册；码/消息/方法表未动。新增 `EngineTier1RemedyFieldTests` 把"文案指到的字段"钉在**真实发布形状**上（含 `gp_probe_status` 提及数 == 2 的 tripwire：新增第 3 处必须自带检查）。
4. **HIGH（已修，我复核后重写）**：TS 侧 trail 的 `reset()`/`record()`/读在并发下按**完成顺序**落地。fix agent 的 trail turn 方案有两处问题，我读代码后自己改：
   - 顺序漏洞：chain 只等"上一个 claim 的 gate"，而被放弃的 turn（engine 调用先失败、从未 acquire）会提前放行 → 后面的 `gp_recent_reports` 仍可能**越过**未落地的 act。改为 session 内维护"未释放 gate 集合"，claim 时快照全部未完成者。
   - 我把门禁改红了：`gp_recent_reports` 的首帧从此排在 `await` 之后，4 个测试（`tools.test.mjs` 3 + `consumer-consistency.test.mjs` 1）依赖"调用同 tick 帧已上线"的前提 → 永久红。修法是把测试前提显式化（命名 `flush()`，注释写明为何只有这个工具晚一个 tick），**没有放宽任何断言**；并补两条新测试：越序释放不得让读越过未完成 act、未应答 act 必须在 engine deadline 到点释放闸（否则一次掉帧把整个证据面永久卡死）。
   - 顺带把 trail 从模块级全局挪进 `EvidenceAuditSession`（请求顺序是一个会话档案的属性），生产里单会话＝行为不变，测试里不再互相卡。

### 我自己另收的两处
`EngineP5Batch3Tests` 的 `makeTempDir` 与"缺失目录"用例改走 `TestSandbox`（该文件内 C-05 自备路径清零），文件尾补换行。

### Round 3 gate 实况（日志 `/var/tmp/gp-iterate-gates/runs/20260923-100232-*`、`20260923-100615-*`）
`gate.sh all` 8 模块 `failed=0`：engine build ✅ / `swift test` **533 通过 0 失败 1 跳过** ✅ / probe **24** ✅ / signal ✅ / bridge **50** ✅ / kernel ✅ / mcp-shell **164 通过 0 失败** ✅ / installer ✅ / root ✅；`GUARD real_projects_json=unchanged`、`~/.glasspane/evidence` 仍 **582** 枚。我随后改测试文件后单独重跑 `gate.sh engine` = 533/0/1 绿（`swift build` 无新增 warning）。

### 规格偏离（不自行改规格，须用户裁决）
`specs/GlassPane_P5_实施规格_v5.0_…md:227-228`（§9.3）字面规定 `pruneEvidence` 两个回落：`evidenceStore?.directory ?? EvidenceStore.defaultDirectory`、`entry.evidenceStoragePath ?? EvidenceStore.defaultDirectory`。round 2 的 A-07/C-06 已把后者改成拒绝，round 3 把前者也改成拒绝 → **§9.3 需要一次修订**，否则"逐字合规"与"测试永不删真实档案"不能同时成立。这是记录在案的第二次同类偏离（第一次是同节 project 分支），不是临时遗忘。

### 残留（下一轮清零，本轮不扩面）
不经 `TestSandbox`、因而不经 `assertIsolated` 的自备 temp 路径还剩 7 处：`ApprovalGateTests.swift:176,229,245,259`、`EngineP1Batch7Tests.swift:46`、`PermissionSubjectTests.swift:685`、`ProjectRegistryTests.swift:10`。它们都在 `$TMPDIR` 之下＝触不到真实状态，但闸不覆盖。下一轮做法：把禁形扩为"测试文件不得自行拼 `NSTemporaryDirectory()`/`FileManager.default.temporaryDirectory`（`TestSupport.swift` 除外）"，一次清零。

### 合流基线（10:05 重新 fetch）
`origin/main` 已从基线 `6f5c0c7` 前进 4 笔到 **b2904f1**（release.yml ×3 + e221fc2 把 `ITERATE.md`/`iterate.config.yaml` 入库）。与本轮 diff 的重叠**只有这两名文件**（add/add）；`README*.md`、`CHANGELOG.md`、`.github/`、`installer/`、`install.sh` 我一个字节没动（风险区口径见 S-6）。

## Round 4 — 业主改口径：「你来，全修掉，然后继续第四轮」（2026-09-23 18:00 起）

授权变化：12 条"需用户裁决"由我逐条定夺并落地；merge/push 仍不在此列。裁决与理由（先读事实，后定，均已在代码/测试里兑现或列入队列）：

| # | 条目 | 裁决 | 已落地证据 |
|---|---|---|---|
| R4-A | `--no-c33` ⇒ `.strong` 永不可达 | 新增第三种输入：**操作员声明**`--unattended-window`。它只替换"没人看 ⇒ 可能污染"这个推断，永不覆盖已抓到的输入；档案里 basis 仍带上监视器状态，`circuitBreaker.reason` 仍带 `input-contamination-not-monitored` 标签 | 3 条新单测 + `.p6_smoke.py` 两条新断言，实测 `PASS NO_ANOMALY 金丝雀：attribution 升 strong level=strong`（这是该主张第一次被端到端测量而不是被断言） |
| R4-B | X-22 `--state-dir` | 落地。状态根＝今天 `NSHomeDirectory()/.glasspane` 那个目录本身；`StateRoot` 是唯一的拼写处，daemon 每进程解析一次，11 个构造点共用；未命名时 stderr 写一行"用了 home 默认" | `.p6_smoke.py`/`.t9_smoke.py` 前置闸从 exit 2 变成 5 条实测判据（含"换回真实 HOME 仍跟着参数走"、"未知参数仍以 64 被拒"）；CLI 实测 `--state-dir /tmp/… --evidence-stats` 报 `dir=/tmp/…/evidence/`；相对路径 exit 64 |
| R4-C | R4-02/R6-01 recipe 契约 | 统一到 **kernel 冻结侧**（字符串 const + `{kind, params}`），并让 Swift 校验器吃同一份真值 fixture | `RecipeLoader` 重写；`RecipeLoaderTests` 15 例逐分支钉错误文本；实测 `--recipe-validate kernel/fixtures/recipe-config.ok-01.json → valid:true`，旧整数形状 → 5 条具体错误 + RC=1 |
| R4-D | §9.3 两处回落 / X-2 act 条件化字段 / X-15 错误码 / R6-12 第 14 工具 / R6-07 审批尾切 / 规格裸字面量 / 0755 存量 / 计数器无读者 / 路径校验不对称 | 见 round3-queue 的"需裁决"清单，裁决口径写入下方"规格批次"与"未落地" | 部分（见下） |

规格裁决口径（R4-D 里我已定的，代码/文档按此走）：审批链纯尾切**不补锚点文件**（同一 uid 可写目录里的锚点防护力为零，是假守卫），改为规格承认不可检 + `--approval-audit` 输出条数与链尾哈希，使"跨时间比较"成为 agent 可执行动作；错误码 `GP_E_ENGINE_TIMEOUT` **提升进 `mcp-shell/src/errors.ts`** 做唯一来源，daemon 枚举不加；路径校验取两侧**并集**并用一份跨语言真值表钉住（不是"统一到某侧"，因为两侧各有更严项）。

### 本轮新发现（复活的闸挖出来的，未修）
- **R4-01（高，须真机/新代码复核）**：`.p6_smoke.py` 隔离与 strong 两段转绿后，卡在 `恢复后 UI 结构回写：count 标记行随快照值 0 清空（差值∈[4,6]） delta=0 before=0 after=0` —— 走到该检查时 demo 的无标识 AXImage 行数本来就是 0，即"回滚前 ≥4"的前提未成立。此前这条从未被执行过（脚本在隔离前置就 exit 2），所以它是**新暴露**而不是新回归。三种可能：(a) 前面的 tier-1 恢复把 count 归零后没再累加；(b) `observe` 序列化不含无标识 Image（`ProbeDemoApp.swift:199` 注释自陈）使该计数恒 0；(c) 本机窗口不在屏（同一 run 里 `pixel-capture-no-onscreen-window` 已实测存在）导致结构面与 AX 树不一致。判据：`smoke.md` 记过"真机 24/24 strong=100%"，所以 (b)/(c) 里至少一条是环境或实现漂移。**下一步必须先分辨 (a)(b)(c) 再动代码**，否则会把一条端到端闸改成"永远能过"。

### 批次 2（提交 2991c95）与本批的自复核

落地：存量权限就地收紧（含 `--state-dir` 把 0700 判定从"在 home 内"改成"看属主"这一处我自己引入的新洞）、
探针三个计数器接到 `probe_status`（缺席≠0）、TS 路径校验并集 + 跨语言清单比较、纯尾切如实 + `tailHash`、
测试自备 temp 路径清零、规格修订记录（新增 1 份修订文件 + 直接改 P5 四处正文）。

自复核（**这是自我复核，不是 iterate 程序要求的独立全新视角复审**，因此不据此宣称收敛）：
按缺陷类别逐问过 —— 越序释放/缺席当测量/守卫能否拒绝/钳制是否回报到调用方/新日志是否真写。
关键一条做了**反向撤销**因果验证：把 `declaresUnattendedWindow` 在 main.swift 里钉成 `false` 重建后，
`.p6_smoke.py` 的 strong 金丝雀退回 `level=weak` FAIL；恢复即转绿 ⇒ 转绿由该改动造成，不是环境巧合。

### 本轮新发现（未修，须下一轮或用户处置）
- **R4-01（高）**：`.p6_smoke.py` 走到"恢复后 UI 结构回写"时 `before=0`（前提"回滚前 ≥4"未成立），三条候选因果待分辨：
  tier-1 恢复把 count 归零后未再累加 / `observe` 不序列化无标识 Image（`ProbeDemoApp.swift:199` 自陈）/ 本机窗口不在屏
  （同 run 实测到 `pixel-capture-no-onscreen-window`）。该检查此前从未被跑到（脚本在隔离前置就 exit 2），属新暴露非新回归。
- **R4-02（中）**：`.p6_smoke.py`/`.t9_smoke.py` 只断言 demo 二进制**存在**，不断言它是**当前构建**。实测：探针包重建前
  demo 报 `caps=['z1']`（缺 z3/checkpoint），端到端闸会静默测旧 SDK。修法：把 demo 自报能力集与 SDK 源码声明的
  能力常量对比（或每次构建后跑），缺一即 NOT RUN(2)。
- 声明式污染是否要一条部署约束（生产 launchd 配置里 `--no-c33` 与 `--unattended-window` 同时出现是否允许）——登记在修订文件末尾。

### Round 5 中段（2026-09-25 上午）：负载判定、像素通道根因、T9 注入失效

- **R5-03 判定完成**：机器空下来（per-core 0.44）后 `gate.sh all` 十步全绿 ⇒ 昨晚三条红是负载导致，非回归。
  门禁脚本因此加了**负载前置**：打印 `LOAD 1m=… cores=… per-core=…`，超 `GATE_MAX_LOAD_PER_CORE`（默认 1.5）
  即 `GATE_NOT_RUN` + exit 97 并给 remedy；`GATE_ALLOW_LOAD=1` 可越权但 LOAD 行照打。两向实测。
- **R5-04（严重，已修）**：`AXChannel` 的 CGWindowList 键写成 `"PID"` / `"Bounds"`（真实为
  `kCGWindowOwnerPID` / `kCGWindowBounds`，本机实测），于是像素通道对任何 app 恒判"无在屏窗口"、
  `pixelDiff` 每枚档案都是 null —— **"没测到"被系统性读成"界面没变化"**。修根因 + 纯函数化选择规则 +
  四例测试（含"喂旧拼法必须选不中"的可变红对照 + 活体交叉核对）+ 冒烟加"本轮至少测出一枚非 null
  pixelDiff"正向断言。实测：修复前 0/7，修复后 **7/7**，`.p6_smoke.py` 首次整条全绿（23 PASS）。
  ⚠ 自纠：r4 我把冒烟的"像素不可用"容忍集扩到含 `pixel-capture-no-onscreen-window`，当时当作环境边界，
  实际是本缺陷的产物 —— 那次扩宽把真故障遮成了已知边界。容忍集保留（无席位机器上合法），
  由上面的正向断言补回鉴别力。
- **R5-05（高，未修，诊断已完成）**：`.t9_smoke.py` 的 T9 检不出退化，根因在**注入侧**：
  24 轮里 fd 净增 0、RSS 只抖动（实测与"什么都不做"的对照组不可区分），原因是裸可执行体
  收不到点击（`actConfirmed=true` 只表示 AXPress 返回 success，**不表示动作执行**）。
  脚本那句"内存 8MB/次 + fd 1/次"是把源码常量当实测念出来。修法两条都要：金丝雀自证并被脚本量到
  （自己 `proc_pidinfo` + fd 计数写进管道，断言改成"注入增量实测 ≥ 阈值"）、以及把投递前提做成真
  `.app` + 上屏确认（daemon 已经说出 `no on-screen window`，今天被当噪声打印）。
  检测侧与阈值都不是原因（采样确在 act 路径喂到、6 样本/双 driver 阈值远低于声称注入率）。
- **产品口径（R5-06，须裁决）**：T9 只在 **act** 路径采样，且 `drivers ≥ 2` 才写 `degradation|`
  ⇒ 只读型 agent（observe/diagnose/last_evidence）永远看不到退化告警；只涨内存或只涨 fd 的单一泄漏
  永远停在 `watch`。"渐进退化检测"的实际覆盖面比 README/规格窄一档：要么按主张扩采样面，要么按实现收主张。

### R5-05 收口（2026-09-25 11:40）：T9 不触发的真正机制与修法

诊断给出的"点击没落地"被本次改动直接证实并修正：按 **title** 选择器（`{role: AXButton, title: "leak"}`）
发 24 次 act，`actConfirmed` 全 true，但进程侧 RSS/fd 完全不动；把按钮改成稳定
`accessibilityIdentifier("leak-button")`、选择器改按 identifier 之后，同一套 act 立刻产生真实泄漏
（金丝雀自报 clicks=15、rss_kb 118016→125904、fds_opened 与 clicks 相等），
T9 在**第 15/24 轮**升 `degradation|memory+handles`，`diagnose.class=T9`，脚本 RC=0。
⇒ `GP_E_*` 层面的 "act 已确认" 与 "handler 执行过" 是两件事，这条现在有实测机制而不只是断言。

脚本侧同时改掉那类"从常量宣读"的写法：
- 金丝雀自报 `clicks/rss_kb/fds_opened` 到文件（`CANARY_METRICS_FILE`），harness 解析；
- 第 3 轮就断言"注入真的发生了"（`clicks ≥ 3`），失败信息点名"问题在点击没到达 handler，
  不在 T9 检测器"，并给出两条自查出路（daemon.log 的 pixel-capture-* 标签、`assert_element` 看 `leak#N`）；
- 最终失败的轨迹打印带上实测注入量，替掉原来那句把嫌疑引向检测器的"见轨迹人工分析"。
标题仍改成 `leak#N` 作为**独立旁证**（AX 树可核，不经自报），选择器不绑它——第一版就因为它绑 title
而让第二轮 act 报 "no element matches the selector"，这个失败本身就成了 title 不稳的现场证据。

### Round 5 收尾（2026-09-25 11:57，合并后树的完整信号）
- 合并后首取可归因信号：`gate.sh all` 十步 failed=0（load per-core 0.68），`swift test` **683 通过 / 0 失败 / 2 跳过**、
  mcp-shell 178、installer 70、probe 24、bridge 58；`GUARD real_projects_json=unchanged`。
- 两条端到端闸在**合并后**的代码上复跑：`.t9_smoke.py` RC=0（第 16/24 轮触发，与 09-19 历史记录吻合；
  新增的产物新鲜度前置 PASS，daemon sha256:15ad88128a81）、`.p6_smoke.py` RC=0（23 PASS，pixelDiff 7/7）。
- R5-06 已按裁决落地（observe 采样 + watch 层进 probe_status，阈值不动）；R5-07（stateDiff 缺席不陈述）
  随编译器警告发现并修；R5-08（axEvent/handlerProbe/responsiveness 三处同形未改）登记未做。
- 上一笔提交的信息与内容不符一处：`eadf434` 说"删掉我的重复测试文件"，实际当时未删（合并把它带回来了），
  合并后 engine-test 因它引用的旧标签 `in:pid:` 编不过 —— 本笔真正删除，并在此明说，不改写历史。

### Round 5 具名余量清账（2026-09-25 12:10）
- **R5-08 已修**：`Classifier.evidenceLine` 里 `axEvent`/`responsiveness`/`crash`/`handlerProbe`
  四处"未测即不提"改为显式缺席陈述（`pixelDiff`/`stateDiff` 早已如此，不对称本身就是坑）。
  两条新用例：全通道缺席 ⇒ 六个词都在，且**不得**出现 `hitCount=0`（缺席≠零命中）；
  已测通道 ⇒ 报数值而不是 unavailable（防"永远说缺席"的退化）。
- **ownership 兜底已落地**（`EngineCore.ownabilityDefect`，接在 `evidenceStoragePathDefect` 尾部）：
  先量后写 —— 实测真实 `DARWIN_USER_TEMP_DIR` 是 `drwx------` 属本用户（放行），
  `/Users/Shared` 是 `drwxrwxrwt root`（世界可写 ⇒ 拒），`/private/var/folders` 是 `drwxr-xr-x root`
  ⇒ 虚构的 `<zz>/<zy>/T/<name>` 被拒。**这条推翻了一条既有期望**（该形状过去在 Swift 侧算合法、
  TS 侧自 B-13 起就拒），属于两侧分裂的正当收敛，不是收紧癖：拒绝理由指名命中的目录、实测模式与 uid；
  正向对照（自己的沙箱必须放行）与被拒对照（0777）各一条，防止做成"谁也过不去"的闸。
- **title 选择器审计（实测计数）**：`"role": "AX…", "title":` 且不带 identifier 的写法在
  `spike/`(2)、`engine/smoke.md`(2)、`.smoke_client.py`(1)、`ProjectRegistryTests.swift`(1) 共 6 处，
  **线上一处没有** ⇒ 不改抄写面，改 agent 真正读的那份：`gp_act` 描述现在写明 identifier 才是稳定句柄、
  `actConfirmed` 只说明动作被接受、并给出自证落地的办法（probe 命中/状态变化/`assert_element` 数值）。
- **installer 的 `process.env.HOME`：核实后不改**。`launchdPlistString` 收到的 socket 路径是被
  显式写进 plist 的 `--socket-path`，daemon 用给定值，不存在两侧各算一遍；残留只是"安装期 HOME
  被指走 ⇒ socket 落在那儿而状态面仍走真实 home"，属已记录的行为混合。台账条目改为"已核实：非缺陷"。
- **zod UTF-16 vs ajv 码点**：不统一。`kernel/src` 是 iterate-skill 主仓的镜像面，本仓单方面改
  会造成镜像漂移；分歧已被 Swift 侧可执行期望钉住（`testAstralNameFollowsTheSchemaAndStillDivergesFromZod`）。
### Round 6 独立复审 + 全量整改（2026-09-25 13:30）

复审范围 `f47e06e..ba07b66`（新视角 agent，区域 A–F）。它的总判断是**不可直接合 main**：
两条"高"＋"中"3、5＋"低"两项。逐条回代码复核后**全部落地**，另加两条本轮自己测出来的新发现。

**先记 ba07b66 的验证账（复审"中"项：那笔改了报告文本与四条判定路径却没有门禁数字）**
- 那笔当时只跑了定向套件：Classifier+Degradation 22/22、ownability 2/2；全镜像门禁未取。
- 本轮补取：13:05 与 13:10 两次 `gate.sh all`，两次都是 `failed=1` 且**同一项** signal（详见下），
  其余 8 项全绿；13:2x 一次 signal 单跑 3/3 绿、`engine-test` 696 tests 0 failures（13:02 那轮，
  含 R6-01 之后的 T9 用例）。合 main 前还要一次 `failed=0` 的全量（机器此刻被另一会话压到 per-core 10+，
  门禁的负载前置在挡，不放水）。

**R6-01（复审"高"①）T9 的窗口与调用频率解耦**。事实核对：三条噪声地板都是"每秒"量
（512 B/s、0.05 fd/s、0.5 ms/s），窗口却按条数（64）剪，`record` 自 R5-06 起也吃 observe 的样本。
⇒ 采样密度＝客户端节奏：50 ms 一次能把整窗压在 3.2 秒内（漏检带泄漏的历史），
也能用 6 个样本的几 KB 抖动凑出两个驱动（假 watch）。修法两条，都不动 `drivers>=2`：
- `record` 拒绝距上一个入库样本 < `defaultMinSampleIntervalSeconds`(0.5s) 的样本，
  `@discardableResult` 返回是否入库（一个看不见自己拒收了多少的节流阀等于没有）。
- `verdict` 增加跨度下限 `defaultMinimumTrendSpanSeconds`(20s)，数值**由地板反解**：句柄最小-change 是 1 fd，
  1/S ≤ 0.05 ⇒ S ≥ 20；内存按 4 KB 页 ⇒ 8；ping 按 1 ms ⇒ 2。取最紧的 20。
用例：`testDenseSamplingIsRefusedAndCountsItself`、`testVerdictIsInvariantToSamplingCadence`
（同一物理泄漏 50 ms 与 1 s 两种节奏必须同判——节流前者为 healthy，是因果证明）、
`testAllocatorJitterOverAShortWindowIsNotTrending`（内含"把跨度闸关掉，同一批数据必须报 degrading"的反向对照）。
既有 T9 夹具的时间基线从 1 s 步长改成 30 s 步长并按比例放大增量（**判据一条未放宽**：tier/drivers/斜率断言原样），
`testFlatSeriesIsHealthy` 与 `testInsufficientSamplesIsHealthy` 补 `judged` 断言，防止"窗口太短"再次冒充"测得健康"。

**R6-02（复审"高"②）`probe_status.degradation` 不许把没测到印成健康**。`tier` 是枚举，样本不足/无跨度/
指标探针缺席全都落 `healthy`；R5-06 把它发出去之后第一次有了读者。现增 `judged` + `basis`（带实测数字）、
`samples/minimumSamples`、`spanSeconds/minimumSpanSeconds` 成对发布，`slopes` 只带真算出斜率的通道
（缺席≠0，沿用探针自报警数的既有口径）。`tools.ts` 的 `gp_probe_status` 描述同步补全（复审"中"③：
它逐字枚举了别的每个键却漏了 degradation ⇒ agent 只会读到 `tier: healthy`）。
新增可变红对照 `mcp-shell/test/probe-status-surface.test.mjs`：从 `EngineCore.probeStatus()` 源码里抽出全部发布键，
逐个要求出现在描述里，并带"抽取必须找到 ≥15 个键 / 两个区域各至少一个 / 造一个未描述键必须报错"的防空转断言。
**这条闸当场抓到两个真漏**：`attachedHasProbe`、`longSession` 从未在描述里出现过（不只是 degradation）。

**R6-03（复审"中"④）自陈式 caveat 到期未删**。`path-consistency.test.mjs` 文件头那段"只比名单不比语义"
写明删除条件是"所引 Swift 原文不再成立"，而 ba07b66 已把它引用的 `contains(stripped)` 换成子树匹配 + ownership 兜底。
caveat 改写为历史 + 把语义本身变成控制：新用例直接读 Swift，要求两条子树分支的 `hasPrefix` 都在、
`ownabilityDefect` 存在且从证据路径检查里可达，并**禁止**旧的 `.contains(` 等值匹配回来。
（写这条时踩到自己定的规矩：Swift 注释里引用了被禁的旧写法，负例先命中了散文 ⇒ 检查前剥 `//` 行，
与 `TestIsolationGateTests` 故意不剥注释的取向相反，两边理由都写进了注释。）

**R6-04 报告面与冒烟面**
- `EvidenceReportGenerator`/`evidence-report.ts`：文档承诺"未测通道打一行 not measured"，六条里只有两条做到
  （`axEvent`/`pixelDiff`/`responsiveness`/`crash` 直接消失）。四条补齐，双语镜像 + 六个字符串逐字对照
  （TS 侧读 Swift 字面量比）。共享金样 `report.ok-01.*` 未动——那枚 pack 六条全测到，这条事实由新增的
  `testMeasuredChannelsNeverRenderAnAbsenceLine` 反向钉住。
  一条既有用例 `XCTAssertFalse(contains("* axEvent:"))` 钉的正是被修掉的缺陷，按"缺席要说出"改成要求那行存在，
  注释写明这是本批的契约变更而非迁就。
- `.t9_smoke.py`：`LEAK_MB_PER_CLICK` 从"失败信息里的宣读"变成注入侧下限判据（金丝雀新增一次 0 击自报作基线，
  实测每击 RSS 增量必须 ≥ 常量一半，否则直接判"泄漏没发生，别怪判据"）；选择器文案不再说 `title='leak'`
  （现在按 identifier，标题是 `leak#N` 见证）；两条 NOT RUN 兜底的 HOME 沙箱断言换成 `os.scandir` 真失败（见 R6-06）。

**R6-05（复审 B(3) 软点）"读不到"不等于"不存在"**。`ownabilityDefect` 原来把 `attributesOfItem` 的任何失败
折成空字典再向父目录爬 ⇒ 一个不可穿越的祖先会替它下面所有路径担保。实测本机：不存在=NSCocoaErrorDomain 260(ENOENT)，
不可穿越=257(EACCES，且该目录下**任何**名字都是 257)。现在只有 260 才继续上爬，其余报"cannot be examined（点名路径与错误）"。
新用例 `testUnexaminableAncestorIsADefectNotAnAbsence` 先实测前置（`contentsOfDirectory` 必须抛错）再判，
并留正向对照（自己拥有的目录下"不存在的孩子"仍合法——那是普通的首次写入）。

**R6-06 本轮自己测出来的两条**
- `AXWindowLookupTests` 的活列表用例把前提写错了：它用"列表非空"当"这台机器有东西可证"。本机实测 24 个在屏窗口
  **全是装饰**（负层 5、Dock 20、Menu 24、StatusItem 25×16、accessory 悬浮球 27），此时"0 个通过层规则"是正确的
  产品行为，断言却报"像素通道又死了"。一个会为桌面状态变红的控制最终就是被人忽略的控制——键名写错能活十天，
  走的正是这条路。改为用 `NSRunningApplication.activationPolicy`（独立于被测规则）划候选集，没有常规应用窗口时
  带直方图 skip；另把实测到的 24/25/27 三层加进合成层规则用例。**当前这台机器该用例是 skip 状态，不是通过**。
- 隔离闸的扩展（本轮清账项）：`TestIsolationGateTests` 新增两条"位置偷看"规则（手写 temp/home 组合），
  豁免名单只有 `TestSupport.swift` 与本文件，且 `testTheExemptionIsLoadBearingAndScoped` 要求每个名字**挣得**它
  （撤掉豁免必须能报出东西）。顺带关掉一条真实后门：`PermissionSubjectTests.uniqueHomePath` 过去在真实家目录下
  造档案目录、写 pack、再 `removeItem`——名字随机所以从不撞车，但它就是本仓为毁掉 projects.json 付过代价的那一族。
  迁到 `TestSandbox.pendingDirectory`；`systemHome` 别名被否决（换个名字躲文本规则＝新洞），
  改成两个谓词 `resolvesUnderRealHome` / `equalsRealHome(_:plus:)`。

**未收口（round 7 起点）**
- signal 冒烟的 6s 边界：`gate.sh all` 里连续两次红（SIGINT 后 6s 未退出），单跑与随后三次门内跑都绿。
  本轮把"红一次说不清是哪一类"修掉了：诊断按 daemon 收尾顺序（先 unlink 再 exit）看现场，
  区分"sigwait 没吃到信号"与"信号已消费、慢在 exit(0)"，并附当时负载；两条 stub 自检保证这个诊断本身可变红。
  **没修的是那两次超时本身**——归因还需要一个能在门内复现的样本。
- 面板侧 `EvidenceReportGenerator` 的姊妹消费者（`ConsoleModel`/`EvidenceTabView` 文本面）只做了渲染层，
  未审"面板显示的缺席与 agent 读到的缺席是否同词"。
- `.p6_smoke.py` / `.t9_smoke.py` 共享段仍是人肉逐字同步（本轮改 `require_current_build` 时手工核对了 6 个函数一致）。
### Round 6 反向撤销因果验证（2026-09-25 13:40，脚本 `/var/tmp/gp-iterate-gates/mutate_r6.sh` + `mutate_m4b.py`）

每条修复单独撤销，要求"声称覆盖它的那条新用例"变红——绿着的复审修复等于没修：

| 撤销 | 结果 | 判读 |
| --- | --- | --- |
| M1 节流阀 0.5s→0 | `testDenseSamplingIsRefusedAndCountsItself` + `testVerdictIsInvariantToSamplingCadence` 4 处红（50 ms 节奏读成 healthy） | 频率解耦确由节流提供 |
| M2 跨度闸 20s→0 | `testAllocatorJitterOverAShortWindowIsNotTrending` 6 处红 | 假警确由跨度闸挡住 |
| M3 不发布 `judged` | `DegradationSurfaceTests` 2 红（发布面两例各一处） | 缺席标记确有读者 |
| M4 stat 失败折回"不存在" | 第一版撤销（`where false`）语义上是空操作，且 `--filter EngineCoreTests` 跑的 21 例里根本没有新用例（它在 `EngineCoreWaveTwoBetaTests`）⇒ **那轮的 rc=0 不能作为结论**；换成真实原形 + 正确类名后 1 红（XCTUnwrap 期望非 nil） | 记录失败的一次，避免把"没跑到"读成"没需要" |
| M5 报告丢掉 `pixelDiff` 缺席行 | `EngineP1Batch4Tests` 2 红（含改过期望的那条既有用例） | 六通道齐是判据而非文案 |

TS 侧那条"逐字比对 Swift 字面量"的用例未做撤销验证：它本身就是两侧取一份比一份的构造，
任一侧改动必然变红（改动面在 Swift 侧已由 M5 覆盖）。
### Round 7 独立复审（只读、不跑构建）＋ round 6b 整改（2026-09-25 14:50）

复审范围 `ba07e66..8a53fa8`（实际哈希 `ba07b66..8a53fa8`，28 文件），agent 明确禁止跑构建/测试，
因为它要给出的是"读代码得到的怀疑"，而当时机器需要留给计时门禁。总判断：**不可直接合 main**。

| 编号 | 复审主张（文件:行） | 我的核实 | 落地 |
| --- | --- | --- | --- |
| 高1 | `EvidenceStore.swift:561` 把列目录失败折成 `[]`，`stats()` 向 `--evidence-stats` 印 0/0 | 真。与 R6-05 同族且在发布面；p6 的隔离前置正好读这一行 | R6-08：三种答案 + `listFailure` 点名目录，CLI 在失败时发 null；两条新用例（含"从未写过的归档仍是事实上的空"正向对照） |
| 高2 | P6 §5(3) 权威键表写 `degradation:{tier,samples,longSession,drivers?}`，实发 11 键 | 真，且是我 25 分钟前自己刚立的表 | 补 6 个键 + `judged:false` 语义 + 指向那条能变红的 surface 闸 |
| 高3 | 隔离闸"豁免必须挣得"只要求两条规则**合起来**命中一次；home 规则 0 命中却整文件豁免 | 真 | 豁免改 `[规则名: [树内相对路径]]`，逐 (规则,文件) 撤销后必须报出东西；另断言本文件不得豁免 home 规则 |
| 中4 | 豁免按 `lastPathComponent` 匹配 | 真 | 同上，改树内相对路径并断言该路径确实在被扫树里 |
| 中5 | 规则只认字面量：`systemTempRoot + …`、`$TMPDIR`、`/tmp`、`mkdtemp` 全躲得过，而 `isolationDefect` 只看 `hasPrefix(temp)` | 真，且是**结构**问题不是 needle 不够多 | 门槛抬到"必须在本进程私有根内"（运行时拒），文本针同时加宽；两条旧期望按新门槛改写，并补"根内但点名生产状态目录"这一档，保证两个拒因各自可达 |
| 中6 | `DegradationSignal.swift:124` 说"每轮≈1 秒、24 轮仍测得到"是从源码常量宣读；各文档留档停在 16/24 | 半真：12/24 我确实有实测（r622 日志），但"≈1 秒"没有时间戳支撑 | 删掉无据的节奏数字，只留有日志出处的触发轮次；`smoke.md` 回归行改为实测 12/24，P2 追加带日期的复跑行（历史 09-19 记录不改写） |
| 低7 | 地板取等：`>=` 让 20.0s 窗口里 1 个 fd 成驱动，与本文件"地板**之上**"的措辞矛盾 | 真 | 三处改 `>` |
| 低8 | surface 闸用裸 `includes(key)`，短键可能被无关散文判为"已描述" | 真（低风险） | 登记未修：本轮先不让它长出新语义，round 8 用 `` `key` `` 形制收 |
| 低9 | `path-consistency` 拿 `for … where … hasPrefix(x + "/")` 的字形当语义判据 | 真（等价重构会假红；危险方向由同文件的负例接住） | 登记为已知形状，不重复上报 |
| 低10 | 两条端到端闸的共享文本只由注释守着，本轮把它从 ~18KB 涨到 ~25KB | 真 | 见下"未做"：本轮只做到"人工核对 5 个函数字节一致"，marker+字节闸没赶上 |

**另外一条本轮自己撞出来的**（不在复审清单里）：signal 闸连着四次红，我上一轮给它加了"看现场"的诊断；
这次诊断直接说出 `sigwait 未消费信号（socket 文件仍在）`，顺着它读到 `main.swift`：waiter 线程在启动
流程**最后一行**才创建，于是 `pthread_sigmask` 与 waiter 之间有一段"屏蔽了但无人消费"的窗口——
过载时这段跑过 6 秒，daemon 看起来杀不掉。R5-07 刚把同类形状从单实例护栏上移除，它在下移一个函数处又存在。
→ R6-07：遮罩装好立刻建线程 + 晚绑定的 `ShutdownSocketRegistry`（只登记自己 bind 成功的名字）。

**已落但复审未列的一条**：低10 的字节闸做了 —— `# >>> SHARED-BLOCK-BEGIN/END` 包住 5 个函数，
`shared_block_problems(sibling)` 比对字节并在 `main()` 第一件事判 NOT RUN(2)；实测两文件段一致
（含中文注释的 UTF-8 字节数相同）。范围只到这对标记（`reject_foreign_checkout` 与 send/recv 仍由
注释守着），这一点写在函数 docstring 里，没让这条闸说自己管了全部共享文本。

**未做/未收口（round 8 起点）**
1. `.p6_smoke.py` 的 NO_ANOMALY 判据：上一轮 5 枚档案 `changedPixelRatio` 恒 0（同 windowId、bounds null）
   而 state/ax/handler 都变 ⇒ T6 是**对的裁决**，红的原因是"像素没拍到变化"。已在失败文案里带出实测数字与负载；
   究竟是被遮/错帧还是采集通路缺陷，等空闲窗口复跑判定。
3. 面板人读面（`EvidenceTabView.swift:286-288`）：stateDiff/crash/handlerProbe 缺席即无卡、
   responsiveness 整块不渲染 ⇒ 与报告面同族的"没测到长得像没事"，复审判定"未修"，本轮只修了渲染器一侧。
4. `EngineCore.swift:488` 同类姊妹处已由 R6-05/R6-08 覆盖到发布面；`ProjectRegistry`/`SocketServer`
   的读侧仍按"读不出即无内容"处理（复审逐点确认那两处不会外发计数），留观。

### R6-11 登记：像素通道在真实负载下报 ratio=0，端到端闸因此红（2026-09-25 14:56，未收口）

`.p6_smoke.py` 的 `NO_ANOMALY 判定` 两次复现同一形状：4 次重试全落 T6（"AX 变了而像素没变"），
5 枚留档档案 `changedPixelRatio` 恒 **0**、`windowId` 全程 1443 不变，而 `handlerProbe.hitCount=1`、
`stateDiff.changed=true`（demo.count 4→5）、`axChanged=true`。

排除与待定：
- **不是**像素通道哑火那一族：`pixelDiff` 是对象、`windowId` 稳定、也没有 `pixel-capture-*` 标签
  （那种情形 ratio 为 null）。
- **不支持**"主线程饿死 ⇒ 重绘晚于 settle"：`pingMs` 0.2–0.5 ms（应答极快），慢的是引擎侧 AX 树摘要
  0.7–2.2 s；`actSettleInterval` 只有 0.15 s，但窗口自己的重绘并不依赖它变慢。
- 今天 12:0x（per-core≈0.7）同一支脚本 23 PASS 且判定到 NO_ANOMALY，所以现象与负载/窗口层叠相关。
- 待定 A：采集实际拿的是**屏幕区域**而非窗口面 ⇒ 目标窗口被别的窗口盖住时，前后两帧拍的都是上面那张
  ⇒ 恒 0。B：采集返回同一帧缓存（若是 B，低负载也该红，与 12:0x 的绿矛盾，概率低但要证）。

下一个实验（接手者先做这个，别靠加重试次数判定）：在**确认前置**的同一 pid 上，围绕一次已知界面变化
把 `captureWindow()` 前后两份 PNG 落盘比字节，并记下 `SCShareableContent` 当时命中的窗口号；再把目标窗口
完全覆盖一次重复。三件事一并定：拍的是窗口面还是屏幕区域、是不是同一帧、windowId 与实际前置是否一致。

`.p6_smoke.py` 的这条失败文案已改成带实测数字（ratio / windowId / state / 当时负载），并写明
`ratio=0` 说的是"前后两帧没差"而不是"界面没变"——这条判读口径不许再退回去。

### Round 6 收口（2026-09-25 16:04）：两条红的真因都不是负载

- **R6-11 像素**：`changedPixelRatio` 恒 0 是因为采集走的是"整屏按窗口矩形裁剪"，遮挡窗口被一起拍了进去。
  一次性定性的实验已入库（`PixelOcclusionDiagnosticTests`，opt-in）：无遮挡 ratio=1.0；被层号 3 的窗口盖住时
  ratio=0.0 且中心像素是遮挡方的绿；改为优先 `desktopIndependentWindow` 后同一实验 ratio=1.0、中心像素是目标自己的蓝。
  ⇒ 分类器此前的 T6（"AX 变了像素没变，疑似渲染层"）是**被假测量误导**，不是应用的渲染缺陷。
- **R6-12 信号**：上一笔我写的"遮罩与 waiter 之间的窗口被负载拉长"是**错的**，本条更正。真因是后台作业控制把
  SIGINT 设成 ignore、daemon 继承 SIG_IGN，被忽略的信号不进 pending 队列，`sigwait` 收不到；0.52–0.6/核的低负载照样红。
  定位用的正是上一笔加的诊断文本（"socket 文件仍在"⇒未消费）。现在闸自己带这一档（`inherit_ignored`），
  修前必红、修后必绿，不再依赖谁碰巧在后台跑门禁。R6-07 的"waiter 提前 + 晚绑定 registry"仍保留（那段空窗是真的，只是不是这四次的解释）。
- 全量门禁 `gate.sh all` **failed=0**（15:58，per-core 0.71，GUARD real_projects_json=unchanged）；
  `swift test` 699 例 / 3 skip / 0 失败；mcp-shell 185/185；`.p6_smoke.py` 与 `.t9_smoke.py` 在最终提交上各 RC=0
  （T9 第 16/24 轮触发，注入侧实测 3 击 +30.3MB ≥ 下限）。
- 合入：`main` 已快进到 `4bb2cdd`（本地分支 `iterate/full-review-20260922` 原样保留）。**未 push**（等口令）。
  仍开着的：面板人读面的缺席渲染（`EvidenceTabView` 无卡＝看不见"没测到"）、surface 闸的 `` `key` `` 形制收紧、
  `path-consistency` 以字形判语义、双失败下"no capture surface available"尚未进标签谱与 p6 的不可用标签表。

## Round Gap（2026-09-25 18:20）：四维精简审查 + 四项 feature-gap 落地

按用户口径不复用 iterate 全 9 维逐轮烧 token，改为**精简四维**并行只读审查：correctness / security /
contract（spec-compliance＋frontend-backend 合并：kernel schema · daemon · mcp-shell 三处镜像）、feature-gap
（用户明确要求：对照 PRD FR-09/10/11/12/16 主动思考可新增功能/服务）。
correctness/security/contract 三维返回的均为既有防护点位（pid 范围校验、帧边界 cap、路径白名单、参数校验、
actor-free no-replay 语义等），**未报告新的原子级缺陷**；feature-gap 经用户批准落地四项。

**feature-gap 现状核查（先看事实，避免重复造轮）**
- `gp_export_evidence`（单包）与 `gp_recent_reports`（聚合 HTML/Markdown）**已实现**，evidence-report.ts 与
  EvidenceReportGenerator.swift 双语言渲染 + golden 锁死。故"美化报告导出"的增量=聚合报告经 REST 暴露。
- `--evidence-stats`、`--recipe-validate` daemon 命令已存在；panel 只读走 socket、写入/一次性走 daemon CLI
  （`runDaemonCLI`），recipe 真源在 kernel/schemas/recipe-config.schema.json。

**四项落地（两个并行实现子代理，目录互不相交；均已过门禁）**
- commit `b04e9f6` feat(mcp)：`mcp-shell/src/http-gateway.ts` 新增 HTTP/REST transport：
  node:http 仅绑 127.0.0.1，复用 `unixSocketEngineClient` 桥接 daemon socket，零新依赖；端点 GET /v1/hello、
  GET /v1/evidence/:operationId、GET /v1/evidence?ids=&format=（单项缺失标 skipped 而非失败）、POST /v1/tools/:name
  （白名单转发，act/restore 保留 no-replay remedy）；鉴权 `GLASSPANE_HTTP_TOKEN`＋常量时间比对，未设置拒绝启动、
  不生成默认 token。tools.ts 为复用导出 parseEvidenceFrame/packForReport。配套 11 条注入式 node:test。
  mcp-shell **196 通过 / 0 失败**。
- commit `2fcf8c9` feat(panel)：`EvidenceStatsView.swift`（证据统计卡：daemon --evidence-stats 读
  {count,totalBytes,listFailure,dir}，分布由本地摘要聚合并如实标注"本地扫描"，daemon 不可达/超时/读不了→显式
  "未测得"），`RecipesTabView.swift`（以 kernel recipe schema 只读真源展示结构＋2 个最小模板，编辑器先 JSON
  可解析检查再调 --recipe-validate 如实回显 {valid,errors[],recipeName}，只写 @AppStorage 不碰项目目录）；
  接入 ConsoleRootView/EvidenceTabView。swift test **702 通过 / 0 失败 / 3 跳过**。

**门禁**：npm run build ✅、npm test --workspaces ✅（mcp-shell 196/0）、swift test 702/0/3 ✅。
**未做（诚实记录）**：HTTP gateway 未接入 npm bundle 产物（bundle 只保留 index.js 的单入口），仅 tsc dist 可用；
  趋势可视化沿用 SwiftUI 原生（未引入 Charts 框架）；`--recipe-validate` 的 errors 无行号（只有 JSON 键路径），
  按 daemon 原样展示。分支未 merge、未 push（等口令）。

## Round 2（2026-09-25 20:48）：代码优先三维审查 + 修复并接入发布链路

用户口径：继续迭代、每轮跑完视情况全线发版；强调**功能缺口审查要有，但代码本身审查同样重要**。
本轮/worktree `iterate/r2-code-20260925-204823`（基线 main d0f2e4b）。三维：
correctness / security / feature-gap，侧重点全部压到上轮新增代码（http-gateway.ts、EvidenceStatsView、
RecipesTabView、tools.ts 导出、bundle/发布链路）。

**审查结论（去重后 13 条真实可辩护缺陷）**
- correctness 7 条：payload 超限错误路径写不回 413（destroy 后写）、GET /v1/evidence?ids= 无数量上限
  （→daemon 请求放大/DoS）、POST /v1/tools/last_evidence 绕过 parseEvidenceFrame（契约漂移把不可信 JSON
  透传，诚实性旁路）、多余路径段静默忽略（/v1/tools/act/junk 仍执行 act）、RecipesTab 校验中模板按钮未禁用
  （过期校验覆盖当前内容）、EvidenceStatsView 越界 level 静默丢弃（分布和≠count 无提示）、index.ts 未调用
  createHttpGateway。
- security 4 条：ids 无上限（与 correctness 重复，合并）、tokenMatches 长度提前短路击穿常量时间声明
  （可探测 token 长度）、内部错误字符串原样回传（信息泄露）、recipe 临时文件 0644 非独占 + TOCTOU。
- feature-gap 9 条，其中 **high：http-gateway 未接入 npm bundle**（单入口删非 index.js + index.ts 不 import +
  无 CLI 启动入口 + 测试只测 tsc dist ⇒ 本轮发版会实际丢掉该功能）——发版 blocker，必须修。

**本轮落地（提交 c1/c2，门禁全绿）**
- c1 fix(mcp)：http-gateway 六缺陷修复 + 诚实新端点 + 发布链路接入。
  A/B/C/D 修（ids 上限 20、token 固定 32B sha+timingSafeEqual 不再提前按长度返回、多余路径段 404、
  POST last_evidence 走 parseEvidenceFrame 解析失败如实 502）；/v1/stats、/v1/recipes/validate 无 daemon
  socket 通道 ⇒ 如实返回 501 GP_HTTP_NOT_PROVIDED（不伪造成功，remedy 指 CLI）；新增
  mcp-shell/src/http-gateway-cli.ts（读 GLASSPANE_HTTP_TOKEN 空则退 2、绑 127.0.0.1、SIGINT/SIGTERM 收尾）；
  package.json 加 bin `glasspane-http` + exports `./http-gateway` + bundle 改三入口（index/http-gateway/http-gateway-cli），
  `find -delete` 白名单豁免；加 bundle 产物冒烟闸（缺失即红）。mcp-shell **204 通过 / 0 失败**（增 9 用例）。
- c2 fix(panel)：EvidenceStatsView 越界 level 归显式"其他(不识别)"档且各档之和==count、新增"无法识别"提示行；
  RecipesTabView 临时文件改 **0600 + O_EXCL + O_NOFOLLOW + UUID(16 次去撞)+ defer 清理**消除 TOCTOU/权限面、
  校验用递增 **token** 门控防止过期结论覆盖当前内容、模板/刷新按钮校验中禁用。swift test **702 / 0 / 3 跳过**。

**门禁**：npm build ✅、mcp-shell 204/0 ✅、swift build/test 702/0/3 ✅、workspaces 全绿。
**规入 backlog（未做，诚实记录）**：FR-10/12"验证覆盖地图"（诚实性强增强，中大，最高优先下轮）；
FR-17 归因强度过滤（EvidenceFilter 缺 strong/weak/soft 维度）；诊断"时间线"视图；多项目资产库（大，低）；
`--recipe-validate` 错误补源 JSON 行/列号（daemon 风险区，跨语言，待下轮）；http-gateway 的 /v1/stats、
/v1/recipes/validate 待未来补 daemon socket 通道后转真。

---

## Round 8（2026-09-26 上午）：MCP 专项审查 → 三条高危 + 门禁补齐

范围按业主口径"下一轮迭代针对项目 mcp 部分"。审查结论是**阻断对外发布**：
"高1/高2/高3 三条都在真实负载下可走到，会把代理引向『杀掉正在执行的 act』或『重复点击』，
而现有门禁一条都不会红"。落地（提交 8493126 之后的 mcp 批）：

- **高1 探针期限与重启判据**（`engine-client.ts`）：`probe_status` 调用方期限 15 s →
  `CALLER_VISIBLE_CEILING_MS`；新增 `LIVENESS_PROBE_OUTCOMES` / `livenessProbeDecision()`
  四结局表，只有 `unreachable` 授权 `--restore-launchd`；`probe_status` 自己超时给专门的
  非循环指引。新闸 `test/probe-liveness.test.mjs`（mock timers 量真实布防期限 + 表上算术不变量）。
- **高2 home 解析同源**（`project-registry.ts`）：`daemonProjectsPath()` 与
  `refuseCandidate()` 从 `os.homedir()`（读 `$HOME`）改为单一出口 `systemHome()`
  （`os.userInfo().homedir`，与 daemon `NSHomeDirectory()` 同语义，实测过）；
  `path-consistency.test.mjs` 加双向闸（剥注释后 ban `os.homedir(`、要求恰一个 `os.userInfo(`、
  并回读 `StateRoot.swift` 的 `NSHomeDirectory()`）。三条新用例改前实测红（`fail 4`）。
- **高3 迟到回复进证据链**：`EngineJsonRpcClient.onLateReply` + `recordLateArrival` +
  `dispatch.ts` 的 `createTrackedMcpServer()`（产品接缝，测试驱动的就是它）；
  `tools.test.mjs` 两条端到端：超时 → 迟到回复 → `gp_recent_reports` 必须列得出来。
- **中4/中5**：`test/method-table.test.mjs`（从 `FrameCodec.swift` 解析 `EngineMethod`，
  凡本壳会发上线的方法必须有**具名**期限，`audit_ui`/`capture_view` 此前一直在吃
  `ENGINE_TIMEOUT_MS*3` 的缺省）；`test/path-consistency.test.mjs` 真值表的系统树行改由
  Swift 两份清单生成（原为手抄 5 个名字，清单实际 8+3 项），例外两侧 realpath 孪生按成员判定。
- **低**：`executableSource` 提到 `test/support/source.mjs` 由两条闸共用（一处定义"代码不是注释"）。

反向变异（`/var/tmp/gp-iterate-gates/mut_r8.py`）：A 期限改回 15 s → 3 红；B' 撤掉产品接缝
的 sink → 2 红（只有端到端用例红，单元用例红不了＝接线本身要靠端到端才看得见）；
C 让 `timed-out` 提重启命令 → 2 红。

## Round 8b（同日）：全新视角复审**上一批修复自身**，又出 4 高

复审者被要求"设法把刚落的改动改红"，结果四条全部成立，其中两条落在我自己刚写的代码上：

- **高1（我的高3 有洞）**：迟到回复按"到达时刻"入链 ⇒ ① 同 app 幂等 `gp_attach` 的
  `reset()` 又把它抹掉（回到 `GP_E_NO_EVIDENCE … run gp_act first`，正是要消灭的重复点击）；
  ② attach 在飞时落地的回复会写进**新**会话链（实测 `['op_OLDAPP']`），违反 `tools.ts`
  自称的请求序不变量。修法：链只在**被 attach 的应用**真的变化时重启（与 daemon
  `if attachedApp != app { history.removeAll() }` 同源，身份键由 `AttachedApp` 的**全部**字段
  构成，由 `test/attach-identity.test.mjs` 回读 Swift 钉住）；每次 `call()` 带链纪元，
  迟到回复落地时纪元不符即**拒绝并说明**（还给出 `gp_last_evidence` 这条回查出路）。
- **高2（我的四结局表里有一句未实测的全称断言）**："engine-error ⇒ 该码自己的 remedy
  永不重启"是假的 —— daemon 给 `GP_E_AX_UNAVAILABLE` 的 remedy 本身就命令
  `--restore-launchd` + `launchctl kickstart -k`（`ProtocolErrors.swift:71`）。改为"按该码自己的
  remedy 执行"，并由 `test/remedy-surface.test.mjs` **从 Swift remedy 表里量出**哪些码命令重启，
  再由闸要求"壳层不得禁止它"（不是我把码名抄进测试）。
- **高3（HTTP 路给出 MCP 路的答案）**：网关自建 client，两个 sink 都没接，却把
  "读 stderr / 进本会话链 / 用 gp_recent_reports 取回"原样发给 curl 调用方 —— 而
  `recent_reports` 带 `execute`，被 `FORWARDABLE_TOOLS` 排除，`POST /v1/tools/recent_reports` 恒 404。
  修法：`ShellSurface`（`mcp`/`http`）进入 remedy 文本（`lateReplyRoute()`），网关注册
  `onEngineNote`/`onLateReply` 到一个真的写 stderr 的 `report` 汇点。
- **高4（同形状残留在 `hello`）**：懒连接下"触发连接的那一帧"先写，`hello` 排在其后，
  15 s 期限使 R4-06 的版本/协议比对在"首请求是 act"时永久丢失，只留一行 stderr。
  `hello` 抬到 ceiling。
- **中5–中8 + 低**：`notifications/cancelled` 与未实现通知必须落一行（并明说"撤销不是撤销回"）；
  `bundleId`+`pid` 改**互斥**（daemon 实测 pid 优先并静默忽略 bundleId ⇒ 会在代理没点名的
  进程上点击，而 advertised `oneOf` 早就是恰好一个）；`errors.ts` 关于 `PAYLOAD_TOO_LARGE`
  "壳层专有"的声称按 daemon 枚举改口，`GP_E_UNKNOWN`/`GP_E_NO_USER_RECORD` 收进同一文件；
  src 内 `GP_E_*` 字面量单一出口闸；4 MiB 三判据对表闸；PNG 预算 × 4/3 ≤ 帧上限的不等式闸。
  `McpServer` 的 `session` 从"可选带默认值"改成运行时必填（默认值让"没接线"看着像正确）。

**10 条反向变异（`mut_r8b.py`）暴露的两条我自己造成的假交付**：
1. `G hello → 15_000` **零红** —— 我给 `hello` 抬了期限却**没有为它写任何闸**（表上不变量只管
   探针）。补 `test/probe-liveness.test.mjs` 两条（握手在 15 s 后仍在飞 + 版本比对真跑起来），
   复跑变异 → 2 红。
2. `C 网关不注册 sink` **零红** —— 追下去发现 `http-gateway.test.mjs` 里我"已经加过"的两条用例
   **从未落盘**（补丁脚本拼接时丢了 `write_text`）。重写后变异 → 红并打印
   "没有 onEngineNote 注册，日志承诺就是空的"。
教训：*泳道报"测试已加"与我自己转述"测试已加"都不算证据，`grep` 文件 + 撤销修复看红才算。*

**泳道报告的失真（逐条回代码复核后驳回）**
- "p6/t9 共享段已用 sha 核对" —— 假：当时只有人读注释，无机器闸（因此本轮真的补了
  `verify_shared_block`，三方向红对照见下）。
- `mcp-shell/schemas/glasspane.evidence-0.1.schema.json` 陈旧并会随 npm 发布 —— 不可复现：
  全仓/全历史/本机可搜索路径内**没有这个文件**，`mcp-shell/schemas/` 是 `bundle` 期从
  `kernel/schemas` 拷出来的构建产物（`.gitignore:13`），当前工作树里根本不存在该目录。
- 报告"我只改这 4 个文件、其中一个是 `test/late-reply-trail.test.mjs`" —— 该文件不存在，
  实际改动落在 `tools.test.mjs` 等 7 个文件里。
- "pid 真值是 `Int(Int32.max)`"这一句**经核对为真**（`ParamValidation.swift:21`，
  `Dispatcher.swift:86` 以 `pidLower...pidUpper` 实际执行该范围）。有一次
  `consumer-consistency.test.mjs` 跑出 1 红（`expected 1000000`，位置在该文件第 421 行的 cap
  对表段）：回代码查清后**不是我的改动** —— 我那一条待插入断言根本没落盘
  （文件内 `pidDeclared` / `pid.lower` 等标识全部为 0 命中），红来自当时仍在写该文件的泳道，
  它写完即消失；此后单跑 26/26、全量 248/248。**教训仍记账**：并行窗口里读到的红，
  必须先确认"我那次编辑到底写没写进去"，再谈归因。

## Round 8c（同日 13:4x）：8b 的修复自身还有第三个洞 + 承诺要带边界

自己回读 8b 的代码时发现（不是泳道报的）：纪元比较写在 `recordLateArrival` **取 trail turn 之前**，
而真正的 `record()` 发生在 `await turn.acquire()` **之后**。于是一次换应用的 `gp_attach` 在飞时落地的
迟到回复 —— 到达那一刻纪元还匹配，随后排在 attach 的 turn 之后写入 —— **照样漏进新链**。先写测试
证红（`recentIds` 里出现 `op_5555…`），再把判定移进 turn（`recordLateArrival(result, generation)` 返回
`LateArrivalOutcome`），并把"撤掉 turn 内的重判"作为反向变异跑通（该用例即红）。提交 `7fa2f45`。

同一类的第二处：两个接入面的 remedy 文本把"迟到回复落地时会写进链"写成了**无条件**承诺，而至少有
四种情形做不到（追踪窗口只留 16 条、中途 re-attach 拒写、帧里没有 operationId、本进程已退出）。
现在句子自带界限（`9e34ee3`），并由 `test/remedy-surface.test.mjs` 断言界限与数字都在 ——
把边界从句子里删掉即红（已实测），因为这类措辞总是先漂回"保证"。

**方法记账**：8b 由全新视角复审抓到 4 条，8c 由自己回读抓到 1 条同一形状的漏洞。
说明"改完就提交"不够 —— 判定发生在哪个时刻、与它保护的写入之间隔着一次 await，
这种错只在把时序画出来看的时候才现形。

## 同批：冒烟脚本与 daemon 的 home 口径（engine 侧）

`real_user_state_root()` 原先用 `os.path.expanduser("~")`（Python 优先读 `$HOME`），而它是
**状态根隔离守卫的判据**：脚本跑在 `$HOME` 被重定向的环境里时，守卫会把沙箱当"真根"，
于是那条"没有任何写入面落在真实根内"的判据当场反过来守着沙箱 —— 毁掉开发者真实
`~/.glasspane` 时一声不响（fail-open，与 71 条变 2 条那次同源）。五份脚本
（`.p6`/`.t9`/`.c33`/`.rebuild_survival`/`.input_monitoring_registration`）统一改为
`pwd.getpwuid(os.getuid()).pw_dir`，读不到记录即 NOT RUN(2)，**不回落 `$HOME`**；
`.p6`/`.t9` 判据 5 顺带被**收紧**（原来"HOME 换回真实值"那一问继承的是被重定向的 `$HOME`，
等于没量到它声称的东西）。实测：`HOME=/tmp/whatever` 下真根仍报
`/Users/ethanlin/.glasspane`；红对照为改前同一实验报 `/private/tmp/whatever/.glasspane`。

新增 `verify_shared_block(__file__)`：两份冒烟脚本按**顶层定义逐名**与兄弟脚本比对
（`ast` 切片），漂移即 NOT RUN(2)；例外表只允许 `DAEMON_CANDIDATES`/`main`，且每条必须
**仍然真的不同**才保留。三方向实测红：漂一个常量 / 把例外抄平（点名 `DAEMON_CANDIDATES`）/
例外表点名一个不共享的定义。正常态：`PASS 共享段已比对：27 个两侧同名定义，0 处漂移`。
覆盖边界（写进注释与规格）：改名或把共享判据搬走不在这条闸视野内。

另修 `.t9_smoke.py` 一处不诚实输出：显式传一个不存在的 `--daemon-bin` 时，它把**每个**兜底
候选都宣判为 `missing artifact`（其中 `.build/debug/glasspaned` 实际存在）。现区分
"显式路径不存在"与候选逐个 present/missing。

## Round 8d（同日 15:2x–16:1x）：复审 8b/8c 的修复，又出 2 高；发版门禁换凭据

两条都落在**上一批刚写的代码**上，都是复现出来的（记录见
`specs/GlassPane_规格修订_2026-09-23_iterate-round4.md` 追加四）。

**8d-高1｜迟到的 `attach` 回复从不重启链。** `restart()` 只挂在"非迟到"分支：daemon 已按换 app 清了历史，
壳层纪元**永不推进**，于是 8b/8c 为跨应用串档准备的每一道纪元判据，在最忙也最容易串档的那条路径上整体
失效，且零日志。复审者给的复现形状（trail 留有前一 app 的 op、`gen=0`、`notes=[]`）与磁盘档案回落合起来
看，后果是把上一个应用的证据渲染进新会话报告。修法：`apply` 回调在 `turn.acquire()` **之后**执行，attach
与非 attach 在同一时刻、同一判据分流；无纪元帧改记 `UNKNOWN_GENERATION` 并默认拒写。反向核对
`if (false && method === "attach")` → 用例红，还原 → `tools` 59/59。

**8d-高2｜"narrow this reply"是关于工具的事实，却写在看不见 schema 的传输层。** 旧实现按"这次请求恰好带了
哪些参数"派生：`gp_observe` 未显式写 `maxDepth` 就被宣判"没有可缩的参数"（它 advertise 1…10）；唯一带
`scale` 的 `capture_view` 那一支根本走不到（PNG 2.4 MB → base64 3.2 MB < 4 MiB，不等式现已钉住）；`scale`
已在下界 0.1 还叫它"再调小"（会被壳层自己的校验拒掉 —— 违反"remedy 必须代理可执行"）。修法：advice 只在
`executeTool` 一个入口经 `Object.create` 影子挂上 `call`，界值全部读自该工具 advertised 的
`inputSchema.properties`，到界的旋钮不再被说成可缩；HTTP 面复用同一函数。反向核对 `const wrapped = engine`
→ 2 红。

**过程记账（我自己的两次失手，都进了代码库又被清掉）**：
① 8b 的 `hello` 期限修复**随代码一起提交却没有门禁** —— 反向变异 `hello: 15_000` 零红才暴露，补了两条测量
（`callerDeadlineMs("hello") === ceiling` + 握手活得比触发它的请求久）。"改完就提交"不够，改完必须问一句
"哪条闸会因为把它改回去而变红"。
② 我的补丁脚本 `src +=` 拼接把两组用例重复粘贴进 `engine-client.test.mjs`（38 例里有 9 个重名）—— 表面全绿，
实际是同一断言跑三遍。**拼接后必须数测试名**，这条已进记忆。

## Round 9（同日 17:4x–20:4x）：审**已发布的 1.3.0**，五维并行报回 40+ 条，成立 20 条（8 高）

审的是 tag `v1.3.0` 那份代码（`cc414cd`）。五个只读 lane：时序与关联 / 跨语言契约 / 建议可执行性 /
**门禁可红性** / 数据与隐私面。全部结论逐条回代码复核（lane 报的 40+ 条里有 20 条成立，其余是误读或
已被别处守住——例如 lane 建议"把探针那条闸改成比较 daemon 侧期限"，方向对但若不配上"清单必须逐名"
就会把 `act` 的 120 s 说成缺陷）。修复与契约记在规格「追加五」，此处只记判断与数字。

八条高危的形状值得记下来，因为它们都不是"逻辑写错"：

- **注释说 A、代码做 B**：id-less 帧的归因注释写"oldest unanswered"，实现是"oldest *unsettled*"。
  于是"act 超时 → 按 remedy 发探针 → daemon 给 act 的无 id 错误"这条路上，act 的错误码与 remedy 被
  当成探针的答案交给探针调用方，探针随后被报成"客户端从没发过"。这是本仓反复出现的同一个洞：**判定
  依据与它声称度量的东西不是一回事**。
- **策略抄了三份**：`act/restore/attach` 不可重放这一判定，传输层、`tools.ts`、`http-gateway.ts`
  各有一份。修 R8d-高2 时只把"建议怎么生成"并到了一处，没并"谁能不能重发"。现在由传输层唯一导出
  `isReplayUnsafeMethod`，两层只留文案。
- **重启指令从两个新口子漏出去**：(a) 帧**已到**但形状不对 → `GP_E_INTERNAL` 的 remedy 里嵌了
  `--restore-launchd`；(b) daemon 侧 `pixel-capture-no-surface` 没有分支，落进 `default:` 那句
  "去授予屏幕录制并重启 daemon"，而壳层原样转发。探针那张四结局表守住了"忙≠死"，没守住"答过≠死了"。
- **修了一半的判据**：`atAdvertisedFloor` 只认 `scale`，于是 `maxDepth: 1` 仍被命令"再调小"，
  跟着就是自家 zod 的一句 `GP_E_BAD_PARAMS`。
- **发布面上没人守的可达性**：网关的"永不监听通配地址"只是文档；token 只查非空；bind 失败打一行日志
  继续活着（真正在服务的是抢下端口那个进程，它顺带收走 bearer token）。
- **注册表通知宣称 daemon 没有 `--state-dir`**——daemon 有，且据此加载 `<root>/projects.json`；
  socket 又确实不回这个字段，所以正确修法不是猜，而是明说看不见 + 给一条读得到的判别路（`launchctl print`）。

**门禁自身（lane D 的功劳最大）**：三条"永远不会红"的对照（探针期限两侧同过 `min(·, ceiling)` ⇒
恒假；`history.removeAll()` 只看第一处；`methodsSentByShell` 看不见 `engine.call(spec.engineMethod, …)`）
已重写；并且新学到一条本仓此前没有的类别——**会挂死的对照不是对照**：删掉 token 门槛后那组用例不红，
而是把事件循环占住（断言"必须抛"却没关没抛时创建出来的 server）。反向变异跑到第 8 条时整个链卡了
9 分钟，才把这件事逼出来。所有"期待拒绝"的用例改成 try/finally 关自己创建的东西。

**中央 gate.sh 的守卫自己也是坏的**：真实 `projects.json` 的基线是 09-23 抓的，之后没人复抓，于是它
每次跑完都比一份陈旧哈希 —— 本轮 17:33 那次绿就伴随 `GUARD real_projects_json=CHANGED` 而
`failed=0`：一个只会嘟囔、不计入否决的守卫，正是"拒绝分支不可执行＝未修"。改为本次运行自抓基线、
变动计入失败并另存前后两份，退出码也终于与 `failed=` 一致；并留 `GP_REG_FILE` 覆盖点，让拒绝分支
可以在**副本**上被证明能红（实测：改副本 → `GUARD_FAILURE` + `failed=1` + 非 0 退出）。

**发版后的账**：1.3.0 的真机两条闸在 17:27/17:29 补到了可归因的绿（每核 1.03 / 0.87），
`gate.sh all` 17:35 `failed=0`（每核 1.14）。所以 1.3.0 的"未经真机端到端验证"这句要收窄成
"跑过且通过、但晚于 tag"；这一条已写回规格追加四的补记与 CHANGELOG 已知边界。

## Round 10（同日 20:4x–23:2x）：审 round 9 的修复本身，成立 12 条（1 高数据面 + 5 高/中文案面）

三个 lane（全新视角复审 / 数据与隐私面 / 门禁可红性）。修复与契约见规格「追加六」。

- **`canonicalJson` 吞掉 `__proto__` 成员（本轮唯一的高危数据面缺陷）**。形状值得记：这不是逻辑写错，
  而是 `{}` 字面量继承了 `__proto__` setter，于是"按 key 赋值"这一行对**某一个 key** 改的是原型而不是
  成员 —— 出站 stdio 帧与转发给代理的 daemon 正文都过这个函数，所以表现为"daemon 发了，代理被告知没有"，
  而 Swift 侧的规范化写手把它当普通字典成员输出，两个接入面对同一份包给出不同字节。两侧都读了才动手
  （`CanonicalJSON.swift` 无特例），修完用四条用例钉住，其中一条**读 Swift**，防的是"以后又分叉"。
- **round 9 刚立的纪律在同批另一条路上破了**：`retryGuidance`（超时路径）又由传输层点名
  "smaller maxDepth"，`maxDepth:1` 的调用方收到的仍是自家 zod 会拒的指令。判据本身没错，错在它只管了
  payload 那一条分支 —— **一条纪律要在每条生成建议的路上都有对应的闸**，否则它会在新路径上原地复活。
- **接线没接到底**：lane A 在传输层给出 `LateReply.attribution`，`audit-session` 会区分
  `late` / `late-inferred`，但 `dispatch.ts` 仍只解构 `{method, result, correlation}` —— 也就是说
  除非把第三参数一路传到生产调用，链上的标注永远走不到。lane 自己在报告里标了这个 handoff，
  没有靠"它说做完了"记账。
- **一条 lane 报告判为不成立**：报"新的总预算没有任何测试提到，改成 Infinity 也全绿"。实测不成立
  （`tools.test.mjs` 有具名用例，且同一位置在 round 9 变异跑里一次红 3 条）。记下来是因为这正是
  [[不要把别的会话/agent 的绿灯红灯当证据]] 的又一例：红与零红都得自己跑过。
- **两个 lane 撞到 subagent 回合上限**，其中 lane C 在报"完成"前被截断、状态未知。收口动作是
  整树 `git status` + `tsc --noEmit` + 全量套件，而不是按报告记账：这次实测它们 6 项里 5 项已落地，
  缺的 `gp_probe_status` 跨面与 `--port=` 之外的两项由主线补完。

**反向变异 12 条全红**（B1 第一次因变异本身编译不过而记 BUILD-FAIL，换成可编译的等价回退后红 4 条）。

## 门禁状态（诚实记录）

**Round 10 之后（同日 22:5x–23:2x，全部可归因）**：`gate.sh all` → `GATE_DONE failed=0`
（LOAD 每核 0.65；`real_projects_json=baseline-taken entries=3` → `unchanged`）；`mcp-shell` 全量
**354 / 354 / 0 失败**；`swift test` **703 个用例 / 3 skip / 0 失败**；installer 70 pass；真机两条闸
在本树 RC=0（`.p6` 24 条 PASS、`.t9` 10 条 PASS；同样是先强制换过一次 daemon 二进制才过新鲜度判据 ——
反向变异脚本的"还原"会把源文件 mtime 顶到构建之后，这是预期的自拒，不是缺陷）。

**Round 9 之后（2026-09-26 20:2x–20:4x，全部可归因）**：`gate.sh all` → `GATE_DONE failed=0`
（LOAD 每核 0.52，max 1.5；9 个模块 exit=0；新守卫 `real_projects_json=baseline-taken entries=3` →
`unchanged`）；`mcp-shell` 全量 **307 / 307 / 0 失败**；真机两条闸在本树重跑（先 `rm -f
.build/debug/glasspaned && swift build` 强制换二进制，因为反向变异的还原把源码 mtime 顶到了构建之后，
两条闸按新鲜度判据自拒 `NOT RUN(2)` —— 方向正确，不是缺陷）：`.p6_smoke.py` **RC=0（24 条 PASS）**、
`.t9_smoke.py` **RC=0（10 条 PASS）**。反向变异 **13 条**：12 条按预期变红，第 8 条（注册表通知的
事实声称）当时零红 —— 那正是它被补闸的原因，补完后同一变异变红（见规格「追加五」§6）。

**Round 8c 时的代码级验证**：`mcp-shell` 全量 `npm test` **248 / 248 / 0 失败**；五份冒烟脚本改动均以
预飞行段实跑（`PASS 共享段已比对…`、`home 口径…`）；13 条反向变异全部达到预期红。

**8d 之后**：`mcp-shell` 全量 **261 / 261 / 0 失败**（`b9f1524`），本轮两条反向变异各自达到预期红后还原。
PR#1（8b/8c 批）已在 `origin/main` 上 **CI 全绿（7 个 job 全过，`00a305f`）**，主检出已快进同步。

**打 tag 当时（1.3.0）未取得、后来取得的**：`gate.sh all` 与两条端到端闸在打 tag 前没有一次可归因的绿
（并行会话把每核 load 顶到 15–33，两条冒烟按新鲜度自拒 `NOT RUN(2)`，那是正确行为**不是通过**）；
17:27–17:35 机器空出来后三条都取到了绿（见上一段），晚于 tag。

**发版门禁换凭据（用户 2026-09-26 选定）**：原计划"打 tag 前必须先拿到 `GATE_DONE failed=0` + 两条冒烟
RC=0"改为**以 GitHub CI 全绿为凭打 tag**。代价写清楚：CI 在 ubuntu runner 上跑，不覆盖真机 AX/屏幕录制路径，
所以 1.3.0 打 tag 那一刻有这两处未经本机端到端验证；补跑已完成并通过，若日后在这两面发现问题按 1.3.1 处理。
